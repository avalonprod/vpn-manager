services:
  app:
    image: app
    ports:
    - "8000:8000"
    - "2000:2000"
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: vpn-manager
    restart: always
    env_file:
      - ../.env
    volumes:
      - ./.bin/:/app/
